import React, { FC } from 'react'

const SearchHeader: FC = () => {
    return (
        <div className="friendListPanel-searchHeader divider">
            <button className="pointer">寻找或开始新的对话</button>
        </div>
    )
}

export default SearchHeader
